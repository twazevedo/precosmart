{
  "name": "precosmart-fullstack",
  "version": "2.0.0",
  "description": "PreçoSmart - Sistema de Comparação de Preços de Eletrônicos",
  "main": "backend/src/server.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install",
    "start": "node backend/src/server.js",
    "dev": "node --watch backend/src/server.js",
    "bot": "node whatsapp-bot/bot.js",
    "bot:install": "npm install whatsapp-web.js qrcode-terminal node-cron --save"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "node-cron": "^4.6.0",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.7"
  }
}
